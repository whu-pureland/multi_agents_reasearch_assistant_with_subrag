# Frontend

Vue 3 + Vite + Tailwind CSS 前端，用于创建研究任务、上传资料、查看 TODO 进度与报告，并通过 SSE 展示实时事件流。

## 运行
- 安装依赖：`npm install`
- 启动开发服务器：`npm run dev`

默认通过 Vite 代理访问后端：`/api -> http://localhost:8000`（见 `vite.config.ts`）。
